# 卡牌、武将、回合与素材完整性审计

审计日期：2026-07-28

## 结论

本仓库既定的早期实体规则集迁移已经完成：标准版、军争篇、神话再临·风、火、林中的普通武将、技能和卡牌均由 TypeScript Core 注册、生成合法动作并结算。此前登记的 37 个 `partial` 扩展技能已全部转为 `complete`。

“完成迁移”在这里指已有产品范围从旧 `Bout / Operate / interpreter` 协议迁入 Core；它不等于新增所有后续官方内容。风、火、林的隐藏神将、山包及更晚版本从未属于当前已安装内容包，仍是独立的内容扩展工作。

发行依据、固定源码提交、素材 URL 和校验方式见 [`img/expansion/shenhua/SOURCES.md`](img/expansion/shenhua/SOURCES.md)。

## 当前可验证规模

| 内容包 | Core 内容 | 规则状态 | 素材状态 |
| --- | ---: | --- | --- |
| 标准版 `standard@0.3.0` | 25 名武将、40 个技能、32 种牌、108 张实体牌 | 全部 `complete` | 牌面和武将图齐全 |
| 军争篇 `maneuvering@1.0.0` | 11 种新增牌、52 张实体牌 | 全部 `complete` | 牌面和装备条齐全 |
| 风 `wind@1.0.0` | 8 名普通武将、11 个技能 | 全部 `complete` | 武将牌和两套头像齐全 |
| 火 `fire@1.0.0` | 8 名普通武将、13 个新增技能，复用标准马术 | 全部 `complete` | 武将牌和两套头像齐全 |
| 林 `forest@1.0.0` | 8 名普通武将、18 个技能 | 全部 `complete` | 武将牌和两套头像齐全 |

合计为 49 名普通武将、82 个不重复技能、43 种卡牌定义和 160 张实体牌实例。

当前自动化证据：

- 16 个 Core 测试文件、134 个场景。
- 卡牌/武将/身份逻辑矩阵覆盖 `43 × 49 × 4`，共 8428 组组合。
- 回合矩阵覆盖 `49 × 4`，共 196 组完整阶段循环。
- 24 个浏览器流程覆盖生产入口、发牌动画屏障、合法牌灰置、目标高亮、响应、濒死、无懈链、判定、通用选择框和 Core/UI 同步。
- 素材脚本固定上游文件修订与 SHA-256，并生成统一尺寸头像。

## 规则迁移

### 卡牌

- 杀、火杀、雷杀、闪、桃、酒进入统一的使用、响应、濒死和伤害管线。
- 标准与军争锦囊统一使用逐目标结算、无懈链、判定区、公共牌池和可恢复工作流。
- 武器、防具和坐骑通过装备槽、距离、时机规则和卡牌移动事件组合，不再由浏览器解释器分支结算。
- 选定范围内 43 种牌没有 `partial` 定义。

### 标准武将

25 名武将引用的 40 个技能全部由 Core 的事件订阅、时机规则、合法动作、视为牌或可序列化工作流实现。

### 风

11 个技能全部完成：

红颜、雷击、鬼道、烈弓、狂骨、神速、据守、天香、不屈、黄天、蛊惑。

新增的通用机制包括跳阶段、翻面与下回合跳过、伤害转移、不屈牌堆、主公技授予以及带质疑流程的视为牌。蛊惑的响应接受会等待质疑工作流结束；假牌或被质疑的非红桃真牌按未响应继续原结算。

### 火

13 个技能全部完成：

强袭、驱虎、节命、连环、涅槃、八阵、火计、看破、天义、乱击、血裔、双雄、猛进。

新增机制包括主动技材料约束、拼点、限定技复位、同花色多材料视为牌、基于标记改变牌色、手牌上限修正和响应后追击。

### 林

18 个技能全部完成：

断粮、行殇、放逐、颂威、祸首、再起、巨象、烈刃、英魂、好施、缔盟、酒池、肉林、崩坏、暴虐、完杀、乱武、帷幕。

新增机制包括来源替换、卡牌目标排除、势力授予技、手牌交换、完杀救援限制、回合外群体响应、体力/手牌条件目标过滤和拼点。

## MatchSetup、快照与浏览器边界

- `MatchSetup` 负责 2–10 人身份分配、主公候选、本地候选、唯一选将和确定性随机种子。
- 主公技在内容定义中显式标记；浏览器构造 Core 玩家时只给主公座次安装这些技能。
- 浏览器入口从 Core 获取身份、武将候选和最终座次；`main.js` 不再调用 `Bout.get_identity / get_hero / get_king_hero`。
- `GameState v3` 快照记录已安装内容包的 ID、版本、规则源码修订和素材 manifest；旧 v1/v2 快照可迁移到 v3。
- `GameSession` 是生产对局的唯一权威状态。浏览器只保留 UI ViewModel 与 Core ID 的映射。
- 生产入口不再定义或暴露 `sgs.Operate`、`sgs.Bout`、`sgs.interpreter` 和旧 AI。

## Legacy 状态

生产入口只加载 `data.js`、`logic_func.js`、`ui_shell.js`、原生 motion、界面投影和 Core bootstrap。旧 `logic.js`、`logic_ai.js`、`logic_interpreter.js` 仅作为历史实现和旧矩阵测试夹具保留，不参与生产运行。

仍保留的 JavaScript 文件职责如下：

- `data.js`：旧矩阵夹具以及 UI 图片/名称展示目录，不再决定生产身份、武将技能、牌堆或规则。
- `logic_func.js`：无规则权威的通用小工具。
- `ui_shell.js`、`main.js`、`interface.js`、`animation.js`：创建和投影 UI ViewModel、提交 Core 命令、消费领域事件。
- `CoreBoutAdapter`：把现有 jQuery 页面适配到 `GameSession`；公开方法直接接收卡牌和目标，不再传递万能 `Operate` 对象。

因此，旧文件仍存在不代表旧协议仍在生产路径。后续若重写 View，可以删除 adapter 和展示目录副本，但那是 UI 替换，不是规则迁移的未完成项。

## 当前范围之外

- 风、火、林各两名隐藏神将及其技能和素材。
- 山包、一将成名、界限突破、国战、SP 和线上版本内容。
- 双将、1v1、3v3、国战等独立模式规则。
- 联网对局、断线恢复、观战和录像播放器。
- 身份推断、多步搜索、MCTS/训练体系。
- 完整语音、每张牌独立演出、无障碍和移动端重做。

这些项目需要新增 ContentPack、模式或产品能力，不是本轮从旧生产规则到 Core 的迁移残留。

## 验收门禁

迁移完成以以下命令全部通过为准：

```bash
npm run typecheck
npm run test:core
npm run test:logic
npm run test:ui
```

此外，生产入口扫描必须确认没有 `sgs.Operate`、`sgs.Bout`、`Bout.get_*`、旧解释器或旧 AI 的定义与调用。
