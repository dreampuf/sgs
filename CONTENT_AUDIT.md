# 卡牌、武将、回合与素材完整性审计

审计日期：2026-07-27

## 审计口径

当前规则集选择三国杀早期实体版本：标准版、神话再临·风、军争篇、神话再临·火、神话再临·林。发行依据、固定源码提交、素材 URL 和校验方式见 [`img/expansion/shenhua/SOURCES.md`](img/expansion/shenhua/SOURCES.md)。

本审计区分三种状态：

- `complete`：已有 Core 定义、规则实现和自动化流程验证。
- `partial`：名称、属性、依赖和素材已进入 Core Registry，但规则尚未完成。
- `missing`：当前 ContentPack 中没有定义或素材。

“能在旧界面选到”不再被视为已实现。浏览器创建对局时会按选中的扩展注册真实 ContentPack；找不到武将定义会直接报错，不再静默创建无技能武将。

## 当前可验证规模

| 内容包 | Core 内容 | 规则状态 | 素材状态 |
| --- | ---: | --- | --- |
| 标准版 `standard@0.3.0` | 25名武将、40个技能、32种牌 | 全部标记 `complete` | 牌面和武将图齐全 |
| 军争篇 `maneuvering@1.0.0` | 11种新增牌；实体52张牌复用标准牌定义 | 11种新增牌全部 `complete` | 11种牌面、3张独立装备条 |
| 风 `wind@1.0.0` | 8名普通武将、11个技能 | 烈弓、狂骨、红颜、雷击、鬼道为 `complete`；其余6个为 `partial` | 8张武将牌和两套头像 |
| 火 `fire@1.0.0` | 8名普通武将、13个新增技能；复用标准马术 | 13个新增技能均为 `partial` | 8张武将牌和两套头像 |
| 林 `forest@1.0.0` | 8名普通武将、18个技能 | 18个技能均为 `partial` | 8张武将牌和两套头像 |

标准版32种牌与军争篇11种新增牌合计43种稳定定义。两个 ContentPack 分别提供108张标准牌和52张军争牌的花色、点数实例；浏览器 Core 对局直接从 Registry 取得所选包的牌堆。

当前自动化证据：

- 15个 Core 测试文件，127个场景。
- 卡牌/武将/身份逻辑矩阵覆盖43 × 49 × 4，共8428组数据组合。
- 回合矩阵覆盖49 × 4，共196组完整阶段循环。
- 14个浏览器流程覆盖合法牌灰置、目标高亮、响应、濒死、无懈链、延时区、判定和通用选择框。
- 素材脚本固定38个上游文件的提交与 SHA-256，并派生48张统一尺寸头像。

这些数字证明已列场景可重复，不代表37个 `partial` 扩展技能已经实现。

## 完整卡牌审计

### 基本牌

| 牌 | 当前实现 |
| --- | --- |
| 杀、火杀、雷杀 | 共用使用次数、攻击范围、目标和闪响应协议；属性伤害、连环传播、酒加伤、无双、铁骑、武器和防具修正均进入同一规则管线 |
| 闪 | 仅作为响应；支持倾国、护驾、八卦阵、无双的多闪需求 |
| 桃 | 出牌阶段恢复、濒死求桃、其他角色救援、急救来源与死亡恢复流程 |
| 酒 | 出牌阶段加伤和濒死自救；状态按下一张杀或回合结束消费 |

### 锦囊牌

| 牌 | 当前实现 |
| --- | --- |
| 五谷丰登 | 公共牌池、按座次逐人选择、逐目标无懈和剩余牌弃置 |
| 桃园结义 | 按存活且受伤目标逐个恢复并进入逐目标无懈 |
| 南蛮入侵、万箭齐发 | 逐目标响应杀/闪、逐目标无懈及后续伤害 |
| 决斗 | 双方交替响应杀、无双修改响应次数、失败方受伤 |
| 无中生有 | 摸两张牌、完整无懈反制链 |
| 顺手牵羊、过河拆桥 | 从手牌、装备区、判定区选择具体牌并移动 |
| 借刀杀人 | 有武器的第一目标、合法攻击的第二目标、出杀或交出武器 |
| 无懈可击 | 可反制无懈，直到全员连续放弃后按奇偶结果恢复或取消原锦囊 |
| 乐不思蜀、闪电、兵粮寸断 | 独立判定区、结算前无懈、改判、命中/未命中路径、跳阶段或传递 |
| 铁索连环 | 一至两名目标、逐目标无懈、横置切换、重铸及属性伤害传播 |
| 火攻 | 目标选择展示牌、使用者选择同花色弃牌、火焰伤害与连环传播 |

### 装备牌

| 分类 | 当前实现 |
| --- | --- |
| 武器 | 诸葛连弩、雌雄双股剑、青釭剑、青龙偃月刀、丈八蛇矛、贯石斧、方天画戟、麒麟弓、寒冰剑、古锭刀、朱雀羽扇均有独立规则；攻击范围和同槽替换共用基础实现 |
| 防具 | 八卦阵、仁王盾、藤甲、白银狮子均有防御/伤害规则；白银狮子离区恢复和伤害封顶已实现 |
| 坐骑 | 绝影、的卢、爪黄飞电、骅骝、赤兔、大宛、紫骍进入统一距离和装备移动规则 |

### 卡牌层仍缺失

当前选定的43种牌没有标记为 `partial` 的定义。仍未完成的是超出这批早期版本的卡牌：

- 后续山包、一将成名、界限突破、国战、SP及当前线上版本新增牌。
- 对应的新关键字、专属标记、模式牌堆和美术素材。
- 后续版本的实体牌清单、花色点数和包依赖尚未录入 ContentPack。

## 武将审计

### 标准版

25名武将引用的40个技能均有 Core 规则：

- 魏：护驾、奸雄、突袭、天妒、遗计、刚烈、反馈、鬼才、裸衣、洛神、倾国。
- 蜀：激将、仁德、武圣、咆哮、龙胆、马术、铁骑、观星、空城、集智、奇才。
- 吴：救援、制衡、反间、英姿、克己、连营、谦逊、奇袭、苦肉、国色、流离、结姻、枭姬。
- 群：无双、急救、青囊、闭月、离间。

这些技能通过通用事件、时机规则、合法行动、虚拟牌或工作流实现，不再由单张牌显式调用。

### 风：5个完整规则、6个缺失规则

已完成：

- 烈弓：订阅闪响应时机，以通用表达式比较目标手牌数、来源体力和攻击范围；发动与放弃进入统一可选效果计划。
- 狂骨：订阅伤害完成事件，以通用距离表达式和事件伤害量恢复体力。
- 红颜：通过统一有效花色查询将黑桃视为红桃，判定、视为技素材过滤和工作流共用同一结果。
- 雷击：订阅闪的响应事件，选择目标后进行黑桃判定并造成2点雷电伤害。
- 鬼道：与鬼才共用参数化改判工作流，限定手牌/装备区黑色牌，并将原判定牌收入手牌。

仍缺：神速、据守、天香、不屈、黄天、蛊惑。

黄忠、魏延的唯一技能已完整实现，因此两名武将标记为 `complete`。张角已有雷击、鬼道，但黄天未完成；其余武将仍明确显示“技能迁移中”。

### 火：13个缺失规则

强袭、驱虎、节命、连环、涅槃、八阵、火计、看破、天义、乱击、血裔、双雄、猛进。

庞德的马术直接复用标准版完整实现，不重复定义。其余13个新增技能为 `partial`。

### 林：18个缺失规则

断粮、行殇、放逐、颂威、祸首、再起、巨象、烈刃、英魂、好施、缔盟、酒池、肉林、崩坏、暴虐、完杀、乱武、帷幕。

### 隐藏神将：6名完全缺失

| 包 | 缺失武将 |
| --- | --- |
| 风 | 神关羽、神吕蒙 |
| 火 | 神周瑜、神诸葛亮 |
| 林 | 神曹操、神吕布 |

这6名武将的数据、技能、AI、武将牌和头像均未接入。

## 回合、模式与状态缺失

当前 Core 已覆盖判定、摸牌、出牌、弃牌、回合结束、死亡玩家跳过、延时牌、濒死救援、击杀奖惩、属性伤害和连环传播。

仍缺：

1. 浏览器入口固定为4人身份局；2–10人身份配置、双将、1v1、3v3、国战等模式未参数化。
2. 身份选择与选将池仍由旧 `main.js` 控制，尚未成为 Core 的 MatchSetup 命令。
3. 保存档已有规则集 ID，但还没有把每个已安装 ContentPack 的来源修订和素材清单写入快照元数据。
4. 多阶段卡牌和技能仍有一部分使用 TypeScript workflow 函数；新基础机制若超出 Ability DSL，仍需要扩展编译器与引擎原子效果集合。
5. 没有联网对局、断线恢复、观战、录像回放播放器和版本兼容迁移策略。

## AI 缺失

当前 AI 只读取玩家视角 `PlayerObservation`，从同一份 `legalActions` 选择动作；支持可替换评分器、确定性决策和 self-play 步进，并会实际响应而非无条件放弃。

仍缺：

- 37个扩展技能的动作生成与估值。
- 身份推断、阵营目标、隐藏手牌概率和风险模型。
- 多步搜索、对手模型、MCTS/强化学习训练和批量对局统计。
- 对复杂工作流、装备取舍、无懈博弈和残局价值的专项评估器。
- 可复现的 AI 强度基准、胜率回归阈值和模型版本清单。

## UI、动画与素材缺失

已完成：

- 不可用手牌预先灰置。
- 合法目标与选中目标高亮。
- 手牌离场、处理区展示、弃置和装备/延时区移动。
- 无懈、横置、判定、濒死、死亡等 Core 事件投影。
- 扩展武将完整牌图统一裁成 `141x144` 与 `137x62` 两种旧头像尺寸。

仍缺：

- 6名隐藏神将素材。
- 全部技能语音、死亡语音和大部分卡牌音效；仓库没有完整声音资源包。
- 43种牌并非都有独立效果动画，目前多张牌共享通用移动/伤害反馈。
- 军争篇5张早期低分辨率牌面只有 `93x130`，与标准版高分辨率源图质量不一致。
- 键盘操作、屏幕阅读器语义、移动端布局和动画减弱模式。

## Legacy 清理清单

生产入口已不再加载 `logic.js`、`logic_ai.js`、`logic_interpreter.js`，但以下桥接仍存在：

- `data.js`：旧测试仍读取其中的武将/牌堆副本；生产 Core 牌堆已改从 ContentPack 获取，但图片映射和扩展选择仍在这里。
- `logic_func.js`：随机与旧工具函数。
- `main.js`、`interface.js`、`animation.js`：jQuery 交互和 Core 事件投影。
- `LegacyPlayer`、`LegacyCard` 与 `CoreBoutAdapter`：旧模型到 Core ID 的双向映射。
- Node 旧逻辑矩阵仍运行旧模型，不能替代 Core 流程验证。

完成清理的判定条件是：选包、组局、选将、牌堆、UI 投影全部直接消费 TypeScript ContentPack、GameSession 和 PlayerObservation，此后才能删除上述桥接，而不是简单删掉仍在提供数据的文件。

## 下一实施顺序

1. 把选包、身份配置和选将迁入 MatchSetup，继续缩小 `data.js` 到纯展示素材映射。
2. 按共用机制优先实现扩展技能：改判与判定触发、伤害转移/恢复、限定技和翻面、拼点、主公技与群体技能。
3. 先完成风包11技能作为第一条真实扩展纵向切片，再迁移火13技能和林18技能。
4. 为每个技能建立合法行动、成功、放弃、无目标、死亡角色、装备/无懈交互和 AI 选择测试。
5. 最后补神将、声音、独立动画和后续版本，避免在规则缺失时用素材制造“已经可玩”的错觉。
