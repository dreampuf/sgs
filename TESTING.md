# 测试说明

## 自动化测试

```bash
npm install
npx playwright install chromium
npm test
```

`npm test` 包含两层测试：

- `test:logic`：在 Node.js 中执行卡牌、阶段、身份、武将、延时锦囊和濒死结算矩阵。
- `test:ui`：启动真实 Chromium，验证玩家可见的完整交互。

也可以单独运行：

```bash
npm run test:logic
npm run test:ui
```

## 浏览器交互用例

| 编号 | 场景 | 操作 | 预期 |
| --- | --- | --- | --- |
| UI-01 | 开始游戏 | 调整扩展包和 AI 难度，点击“开始” | 开始按钮和选项隐藏；选将框显示；只加载勾选的扩展 |
| UI-02 | 主动出杀 | 选择“杀”，选择合法目标，点击“确定” | 确定按钮只在选足目标后出现；手牌减少；进入“闪”响应队列 |
| UI-03 | 自用牌 | 受伤后选择“桃”，直接点击“确定” | 不要求手动选择自己；手牌减少；恢复一点体力 |
| UI-04 | 全体锦囊 | 选择“南蛮入侵”，直接点击“确定” | 不要求手动选择目标；手牌减少；正确进入响应队列 |
| UI-05 | 响应专用牌 | 出牌阶段选择“闪”或“无懈可击” | 不出现确定按钮，不消耗牌 |
| UI-06 | 一至两目标 | 使用铁索连环，选择/取消/重选一至两名角色 | 达到最少目标后可确认；取消至零目标后不可确认；所选角色横置 |
| UI-07 | 决斗 | 选择决斗和一名目标并确认 | 手牌减少；进入双方交替出杀的响应链 |
| UI-08 | 五谷丰登 | 选择五谷丰登并确认 | 牌不会静默消耗；当前简化结算为每名存活角色获得一张 |
| UI-09 | 被动响应 | 收到“闪”请求，选择“闪”，点击“确定” | 使用响应接口；手牌减少；响应队列清空；控件复位 |
| UI-10 | 取消响应 | 收到“闪”请求，选牌后点击“取消” | 不消耗手牌；提交无响应；控件和选中状态复位 |
| UI-11 | 濒死救援 | 玩家处于0体力并收到求桃请求，选择桃确认 | 回复至1体力；清空同批求桃请求 |
| UI-12 | 死亡结算 | 玩家处于0体力并取消最后一个求桃请求 | 标记死亡；清空请求队列 |
| UI-13 | 无需弃牌 | 出牌阶段手牌数不大于体力，点击“弃牌” | 立即结束当前回合并推进到下一存活玩家 |
| UI-14 | 需要弃牌 | 手牌数大于体力，点击“弃牌”，选足牌后确认 | 所选牌进入弃牌堆；按钮复位；推进回合 |
| UI-15 | 不可用牌预灰 | 进入出牌阶段；手中有响应牌、满血桃和无合法目标锦囊 | 这些牌在点击前已灰置并标记不可用 |
| UI-16 | 合法目标高亮 | 选中过河拆桥 | 所有合法角色绿色高亮；点击后改为红色“已选择” |
| UI-17 | 手牌离场与弃牌动画 | 使用桃并确认 | 对应 DOM 立即离开手牌区；桌面展示后进入可见弃牌堆 |
| UI-18 | 延时锦囊状态 | 使用并结算乐不思蜀 | 目标判定区显示状态；判定时退出并展示判定牌 |
| UI-19 | 无懈反馈 | 用无懈可击响应过河拆桥 | 响应牌离开手牌；明确显示被抵消牌名和目标 |
| UI-20 | 扩展武将头像 | 将曹仁分别放入本地和 AI 角色位 | 选将仍用整卡图；对局改用 141×144 和 137×62 裁切头像 |

每个浏览器用例还会收集未捕获的 JavaScript 异常；出现异常即判定失败。

## 逻辑矩阵规模

| 测试文件 | 覆盖范围 |
| --- | --- |
| `content-matrix.test.js` | 43种牌 × 49名武将 × 4身份，共8428个选择和主动使用组合；同时验证数据、图片与技能说明 |
| `card-rules.test.js` | 22件装备逐件入槽；杀/闪、酒、AOE、决斗、借刀、五谷、桃园、顺手、拆桥、火攻、铁索等具体结算 |
| `delayed-rules.test.js` | 乐不思蜀、兵粮寸断、闪电 × 4身份；闪电命中/传递；延时锦囊无懈 |
| `dying-rules.test.js` | 4种击杀者身份 × 4种被击杀者身份；桃救援；反贼击杀奖励；主公误杀忠臣惩罚 |
| `turn-matrix.test.js` | 49名武将 × 4身份的判定、摸牌、出牌、弃牌；死亡跳过；跨回合状态；弃牌堆重洗 |
| `hero-matrix.test.js` | 49名武将 × 4身份 × 3个 AI 难度；83个武将-技能挂载（82个不同技能）逐个声明实现状态 |
| `smoke.test.js` | 无懈旧请求崩溃、AI 身份策略及关键军争牌的快速回归 |

## 重点逻辑与数据用例

| 编号 | 场景 | 输入/前置状态 | 预期 |
| --- | --- | --- | --- |
| LOGIC-01 | 扩展包幂等加载 | 连续加载同一个扩展包两次 | 第一次成功，第二次拒绝重复追加 |
| LOGIC-02 | 真实早期包清单 | 检查风、火、林和军争篇 | 三个武将包各有8名普通武将；军争篇有52张牌 |
| LOGIC-03 | 扩展素材完整性 | 遍历所有扩展武将图和牌图 | 每个声明的本地文件均存在 |
| LOGIC-04 | 基础素材映射 | 遍历装备条、阵亡身份图 | 映射文件全部存在；青釭剑攻击范围为2 |
| LOGIC-05 | 困难 AI 身份倾向 | 分别以反贼、忠臣计算攻击权重 | 反贼优先主公；忠臣避免主公 |
| LOGIC-06 | AI 保留关键牌 | 比较忠臣持有的桃和杀 | 桃的保留价值高于杀 |
| LOGIC-07 | 无懈结算清队列 | 两名角色都已收到同一锦囊的无懈询问，其中一人打出无懈 | 原锦囊出栈；同批剩余无懈询问全部失效 |
| LOGIC-08 | AI 收到过期无懈询问 | `bout.opt` 已空但旧请求到达 AI | AI 安全拒绝，不读取空栈的 `target`，不抛异常 |
| LOGIC-09 | 酒后属性杀 | 先用酒，再用火杀，目标不出闪 | 造成2点伤害，酒增伤状态被消费 |
| LOGIC-10 | 兵粮寸断判定 | 梅花以外的判定结果 | 目标获得跳过摸牌阶段状态 |
| LOGIC-11 | 铁索连环基础状态 | 对未横置目标使用铁索连环 | 目标切换为横置状态 |
| LOGIC-12 | 延时锦囊生命周期 | 放置、判定或被无懈 | 判定牌和延时牌进入弃牌堆；栈关闭；闪电未命中时传给下家 |
| LOGIC-13 | 濒死与死亡 | 0体力角色依次求桃 | 桃可救援；最后无人出桃时完成死亡并丢弃区域内牌 |
| LOGIC-14 | 身份击杀奖惩 | 击杀反贼；主公击杀忠臣 | 前者摸三张；后者弃置所有手牌和装备 |
| LOGIC-15 | 跨回合状态 | 回合结束并推进 | 清除出杀/酒/乐等临时状态，保留铁索横置状态，跳过死亡玩家 |
| LOGIC-16 | 弃牌堆重洗 | 牌堆不足且弃牌堆有牌 | 只重洗弃牌堆，不复制一副全新牌 |
